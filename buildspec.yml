version: 0.2

phases:
  build:
    commands:
      - npm install
      - npm run webpack
  install:
    runtime-versions:
      nodejs: 10
artifacts:
  files:
    - index.html
    - favicon.ico
    - Jeff_Haineault_Resume.pdf
    - styles/main.css
    - images/*
    - dist/bundle.js
