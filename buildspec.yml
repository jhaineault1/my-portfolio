version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10
  build:
    commands:
      -echo Nothing to do yet
artifacts:
  files:
    - index.html
    - favicon.ico
    - Jeff_Haineault_Resume.pdf
    - styles/main.css
    - images/*
